# Core Django
django==4.2.16
django-unfold==0.84.0
gunicorn==23.0.0
djangorestframework==3.15.2
django-cors-headers==4.4.0
python-dotenv==1.0.1
psycopg2-binary==2.9.9
whitenoise==6.7.0

# LangGraph + LangChain
langgraph-supervisor==0.0.10
langchain==0.3.7
langchain-openai==0.2.8
langchain-community==0.3.7
langgraph>=0.3.5,<0.4.0

# OpenAI
openai==1.54.3

# Pinecone
pinecone>=7.3.0,<8

# Celery + Redis (disabled on Vercel — works locally)
celery==5.4.0
redis==5.2.0
django-celery-beat==2.7.0

# HTTP
requests==2.32.3
httpx<0.28.0

# mysqlclient — LOCAL ONLY (needs native MySQL libs, breaks on Vercel)
# Install manually: pip install mysqlclient
# Only needed when USE_DUMMY_DATA=False

# playwright — LOCAL ONLY (needs browser binaries, breaks on Vercel)
# Install manually: pip install playwright && playwright install chromium
# Only needed to run the RAG scraper
